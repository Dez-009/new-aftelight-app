# Railway Production Environment Variables
# Copy this to your Railway dashboard environment variables

# Application
ENVIRONMENT=production
DEBUG=false
APP_NAME=AfterLight API
VERSION=1.1.0

# Server (Railway automatically sets PORT)
HOST=0.0.0.0

# Security (CHANGE THESE IN PRODUCTION!)
JWT_SECRET=your-super-secret-production-key-change-this-immediately
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=30
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database (Railway PostgreSQL service will provide this)
# DATABASE_URL will be automatically set by Railway when you add a PostgreSQL service

# Redis (Optional - for rate limiting and caching)
# REDIS_URL=redis://your-redis-service-url

# OpenAI (Required for AI features)
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4
OPENAI_MAX_TOKENS=2000

# File upload
MAX_FILE_SIZE=5242880
UPLOAD_DIR=uploads
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp

# Rate limiting
RATE_LIMIT_WINDOW=60
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# External services (Optional)
# SUPABASE_URL=your-supabase-url
# SUPABASE_KEY=your-supabase-key

# Email (Optional - for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Print services (Optional - for keepsakes)
# PRINTFUL_API_KEY=your-printfull-api-key
# LOB_API_KEY=your-lob-api-key

# CORS (Update with your frontend domain)
ALLOWED_ORIGINS=https://afterlight.app,https://www.afterlight.app,https://your-frontend-domain.com

# Trusted hosts
ALLOWED_HOSTS=afterlight.app,www.afterlight.app,your-frontend-domain.com

# Railway-specific (automatically set)
# PORT - automatically set by Railway
# RAILWAY_ENVIRONMENT - automatically set by Railway
# RAILWAY_PROJECT_ID - automatically set by Railway
# RAILWAY_SERVICE_ID - automatically set by Railway
